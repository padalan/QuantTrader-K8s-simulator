apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "base-microservice.fullname" . }}-test-connection"
  labels:
    {{- include "base-microservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "base-microservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
